/*
Deployment script for M2-Project

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "M2-Project"
:setvar DefaultFilePrefix "M2-Project"
:setvar DefaultDataPath "C:\Users\James Chan\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\James Chan\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[Posts_Employee].[BarangayName] is being dropped, data loss could occur.
*/

IF EXISTS (select top 1 1 from [dbo].[Posts_Employee])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Altering Table [dbo].[Posts_Employee]...';


GO
ALTER TABLE [dbo].[Posts_Employee] DROP COLUMN [BarangayName];


GO
PRINT N'Altering Procedure [dbo].[spPosts_Detail]...';


GO
ALTER PROCEDURE [dbo].[spPosts_Detail]
	@id int
AS
begin
	SELECT [p].[Id], [p].[Position], [p].[CurrentPay], [u].[LastName], [u].[FirstName], [p].[DateJoined]
	FROM dbo.Posts_Employee p
	INNER JOIN dbo.Barangay_Employee u
	ON p.AdminId = u.Id
	WHERE p.Id = @id;
end

RETURN 0
GO
PRINT N'Altering Procedure [dbo].[spPosts_Insert]...';


GO
ALTER PROCEDURE [dbo].[spPosts_Insert]
	@adminId int,
	@position nvarchar(50),
	@lastName nvarchar(50),
	@firstName nvarchar(50),
	@currentPay decimal(18, 2),
	@dateJoined datetime2
AS
begin
	INSERT INTO dbo.Posts_Employee
		(AdminId, Position, LastName, FirstName, CurrentPay, DateJoined)
		VALUES (@adminId, @position, @lastName, @firstName, @currentPay, @dateJoined)
end
GO
PRINT N'Altering Procedure [dbo].[spPosts_List]...';


GO
ALTER PROCEDURE [dbo].[spPosts_List]
AS
begin
	set nocount on;

	SELECT [p].[Id], [p].[Position], [p].[CurrentPay], [u].[LastName], [u].[FirstName], [p].[DateJoined]
	FROM dbo.Posts_Employee p
	INNER JOIN dbo.Barangay_Employee u
	ON p.AdminId = u.Id
end
GO
PRINT N'Altering Procedure [dbo].[spPosts_Update]...';


GO
ALTER PROCEDURE [dbo].[spPosts_Update]
    @adminId INT,
    @position NVARCHAR(50),
    @lastName NVARCHAR(50),
    @firstName NVARCHAR(50),
    @currentPay DECIMAL(18, 2),
    @dateJoined DATETIME2
AS
BEGIN
    UPDATE dbo.Posts_Employee
    SET
        AdminId = @adminId,
        Position = @position,
        LastName = @lastName,
        FirstName = @firstName,
        CurrentPay = @currentPay,
        DateJoined = @dateJoined
    WHERE
        Id = @adminId;
END
GO
PRINT N'Refreshing Procedure [dbo].[spPosts_Delete]...';


GO
EXECUTE sp_refreshsqlmodule N'[dbo].[spPosts_Delete]';


GO
PRINT N'Update complete.';


GO
